{
  "name": "@legal-exhibits/mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for legal exhibit PDF processing - split, merge, and optimize PDFs",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "legal-exhibits-mcp": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "worker:dev": "wrangler dev",
    "worker:build": "workers-mcp docgen src/index.ts",
    "worker:deploy": "workers-mcp docgen src/index.ts && wrangler deploy"
  },
  "dependencies": {
    "@mistralai/mistralai": "^1.10.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pdf-lib": "^1.17.1",
    "workers-mcp": "^0.0.13",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.6.2",
    "typescript": "^5.3.2",
    "wrangler": "^4.53.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
